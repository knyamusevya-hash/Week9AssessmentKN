#Use the template below to get started

url = endpoint
response = requests.get(l)

if response.status_code == “ok”:
data = response.json()
else:
print(f"Error code: {“error code”}")
name = data[index of name]
print(name)


return import
url = endpoint:
    url= (f"https://api.nationalize.io/?name=nathaniel")
    response = requests.get(1)
    if response.status_code == “ok”:
data = response.json()
else:
print(f"Error code: {“error code”}")
name = data[index of name]
print(name)